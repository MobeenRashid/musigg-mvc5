@model Musicly.Core.ViewModel.GigFormViewModel
@{
    ViewBag.Title = "Create";
}

<div class="gg-login-form col-md-4 col-md-offset-4">
    <h2>Add a Gig</h2>
    @Html.Partial("_GigFormView", Model)
    <button name="btnSave" class="btn btn-primary btn-lg" onclick="$('#gigCreateForm').submit();" style="font-size: 16px; height: 44px">Save</button>
</div>@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}
